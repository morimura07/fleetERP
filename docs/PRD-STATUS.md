# PRD Module Status — Transport & Logistics ERP

Maps the implementation against the contracted scope in `ERP_PRD_v1.0.pdf` (Mori / Oyatech Japan): **36 modules, 6 phases, 52 weeks, 6 countries**.

> **Legend:** ✅ Done · 🟡 Partial · ⬜ Not started
>
> _Last reviewed: 2026-07-31. Source of truth is the code; this doc is regenerated by hand — re-verify before client sign-off._
>
> **Architecture note:** the app is split into two independently-deployable services — `frontend/` (Next.js web) and `backend/` (standalone Hono API that owns all DB access). The API is Hono route handlers (not Next.js Route Handlers). Several cross-cutting tables are **duplicated** and must be kept in sync: RBAC (`backend/src/lib/rbac.ts` authoritative, `frontend/lib/rbac.ts` mirror), Zod validations (`backend/src/lib/validations.ts` ↔ `frontend/lib/validations.ts`), and enum/label maps (`frontend/lib/enums.ts`, `frontend/lib/labels.ts`).
>
> **Multi-company (2026-07):** single-DB multi-tenancy is live. A `Company` registry defines each legal entity; a `dataAreaId` string column partitions every business record and is **enforced at the query layer** (`backend/src/lib/scope.ts` — `areaScope`/`areaForWrite`/`assertSameArea`). ADMINs get a cross-entity view with a company switcher (`X-Data-Area` header).

## Snapshot

| Metric | Value |
|--------|-------|
| Prisma models | 76 |
| Migrations applied | 32 |
| Admin screens | 45 |
| RBAC permissions | 96 |
| Roles | 5 (ADMIN, DISPATCHER, FINANCE, DRIVER, STAFF) |
| Unit tests | 255 passing |

## Phase status

| Phase | Scope | Status |
|-------|-------|--------|
| **1 — Foundation** | RBAC, GL, basic AP/AR, multi-company, audit | ✅ Done (see foundation gaps below) |
| **2 — Core Finance** | Budgeting, Cash & Bank, Tax, Consolidations, multi-currency | ✅ Done |
| **3 — Operations** | Fleet, Transport, GPS, Common | ✅ Done (live GPS feed integration-gated) |
| **4 — Inventory & Payroll** | Inventory, Warehouse, Procurement, Payroll, Expense | ✅ Done |
| **5 — Assets & HR** | Fixed Assets, Service Mgmt, HR, Time & Attendance | ✅ Done |
| **6 — Commercial & Dashboard** | Sales, Retail/POS, Projects, Cost Acct, Master Planning, Dashboard | ✅ Done (in-scope modules; vendor portal / production control / questionnaire remain out of logistics scope) |

## Module-by-module (M1–M36)

### Core Financial (M1–M10)

| # | Module | Status | Notes |
|---|--------|--------|-------|
| M1 | Accounts Payable | ✅ | Vendor master (TIN/VRN), payment terms, WHT, ledger posting. 3-way PO↔receipt↔invoice matching now available via Procurement (M15). |
| M2 | Accounts Receivable | ✅ | Customer registry, credit limit/days, tax-exempt, demurrage-per-trip. **E-invoicing (KRA TIMS/TRA VFD) not integrated.** |
| M3 | Budgeting | ✅ | CapEx/OpEx lines per cost center; STRICT_BLOCK / WARNING_ONLY / OVERRIDE; consumed wired into trip-expense & AP posting (account-level). |
| M4 | Cash & Bank | ✅ | Bank / mobile-money / cash accounts; driver disbursements with PENDING→SUCCESS\|FAILED\|TIMEOUT lifecycle, ledgered on settle. **Live M-Pesa/Airtel & bank-feed APIs are stubs.** |
| M5 | Consolidations | ✅ | Subsidiary→parent account mapping, rate-type rollup, unmapped/missing-rate flags. |
| M6 | Cost Accounting | ✅ | Per-trip P&L (`computeTripPnL`) + fuel efficiency per vehicle; per-contract/project P&L via Projects (M29); per-corridor profitability (`getCorridorProfitability`, Corridor P&L screen); standard-cost variance via M13 (`getCostVarianceReport`). |
| M7 | Credit & Collections | ✅ | Aging buckets (`agingBucket`) + collections workspace. Full dunning ladder (NONE→REMINDER→…→LEGAL), dispute lifecycle (OPEN→UNDER_INVESTIGATION→RESOLVED/WRITTEN_OFF), promise-to-pay, contact log, and bad-debt write-off (posts Dr 6200 / Cr 1100). |
| M8 | General Ledger | ✅ | Double-entry, immutable POSTED + contra-reversal, dimensions, per-posting FX rate. Period-end multi-currency revaluation restates open foreign-currency AP/AR to the period-end rate and posts the net unrealized FX gain/loss (Dr/Cr 4900/6900) — preview then post from the Exchange Rates screen. |
| M9 | Payroll | ✅ | Monthly pay runs (DRAFT→APPROVED→POSTED) with country statutory computation (PAYE bands, NSSF, SHIF) and ledger posting. Approved timesheet overtime (M26) is added to gross as taxable earnings. |
| M10 | Tax | ✅ | VAT/WHT return prep, country tax-component table (TZ/KE/UG/RW/ZM). **Automated filing-period export partial.** |

### Operations & Logistics (M11–M12)

| # | Module | Status | Notes |
|---|--------|--------|-------|
| M11 | Fleet Management | ✅ | Vehicle registry + maintenance + insurance/inspection/COMESA/Yellow Card expiry; driver documents (licence/passport/Yellow Fever/COMESA); fuel-efficiency targets & monitoring. Unified compliance dashboard with 14-day warning window. |
| M12 | Transportation Mgmt | ✅ | Orders, Trips, corridors, weight/CBM, Trip P&L, freight-bill reconciliation (Unreconciled/Matched/Discrepancy). **Live GPS tracking is integration-gated** (positions modeled, feed stubbed). |

### Inventory & Supply Chain (M13–M18)

| # | Module | Status | Notes |
|---|--------|--------|-------|
| M13 | Cost Management | ✅ | Moving-average inventory valuation drives cost; per-trip P&L (M6). **Standard-cost variance**: set a standard cost per stock item and the Cost Variance report (`getCostVarianceReport`) shows actual (moving-average) vs standard — per-unit and on-hand value variance — plus realized purchase price variance from receipts. |
| M14 | Inventory | ✅ | Stock items (spare parts/fuel/tyres/…), moving-average cost, receipts/issues/adjustments with ledger posting (Dr expense / Cr inventory). Issues feed vehicle service orders (M22). |
| M15 | Procurement | ✅ | Purchase orders (DRAFT→APPROVED→PARTIAL→RECEIVED→CLOSED), goods receipts, 3-way PO↔receipt↔invoice matching with variance flags. |
| M16 | Product Information | ✅ | Stock-item master with category/unit, plus a **product-attribute catalog**: define custom specs (`ProductAttribute` — TEXT/NUMBER/BOOLEAN/LIST, optional unit, scoped to a stock category or all) and set a value per item from the inventory Specs editor (`StockItemAttribute`). |
| M17 | Vendor Collaboration | ⬜ | (vendor portal not in scope yet) |
| M18 | Warehouse | ✅ | Multi-warehouse per-location balances, default warehouse, inter-warehouse transfers (TRANSFER_OUT/IN). |

### Asset Maintenance & Workshop (M19–M22)

| # | Module | Status | Notes |
|---|--------|--------|-------|
| M19 | Asset Management | 🟡 | Covered by Fixed Assets (M20) register + Fleet (M11) maintenance. **No custody/assignment or warranty-schedule tracking.** |
| M20 | Fixed Assets | ✅ | Asset register (vehicle/equipment/…), straight-line depreciation runs posting Dr Depreciation Expense / Cr Accumulated Depreciation, and disposal with gain/loss (4910/6910). |
| M21 | Production Control | ⬜ | (not applicable to a logistics operation) |
| M22 | Service Management | ✅ | Workshop service orders (OPEN→IN_PROGRESS→COMPLETED→POSTED); parts issued from Inventory (M14), labor lines; external-garage labor accrues to the ledger, internal labor is a memo cost. |

### Human Capital (M23–M26)

| # | Module | Status | Notes |
|---|--------|--------|-------|
| M23 | Expense Management | ✅ | Trip cash-sheets / expense claims (DRAFT→SUBMITTED→APPROVED→POSTED) with per-line receipt uploads, reconciled against driver advances, posted to the ledger. |
| M24 | HR | ✅ | Employment contracts (one ACTIVE per employee, salary sync on activation), leave management (working-day count, per-year balances, approve/cancel with balance refund), employee documents with 14-day expiry warnings. |
| M25 | Questionnaire | ⬜ | (not in current scope) |
| M26 | Time & Attendance | ✅ | Clock-in/out time entries rolled into monthly timesheets (regular vs overtime over 176h/mo). Approved overtime feeds Payroll (M9) gross. Biometric-terminal ingestion integration-gated. |

### Commercial & Retail (M27–M29)

| # | Module | Status | Notes |
|---|--------|--------|-------|
| M27 | Sales & Marketing | ✅ | CRM leads (NEW→…→WON/LOST) and freight quotations (`QUO-` sequence) with priced line items. An ACCEPTED quote converts atomically into a freight Order (M12), carrying the quoted total as freight revenue. |
| M28 | Retail / POS | ✅ | Over-the-counter sales of stock items (`POS-` sequence). Completing a sale relieves inventory at average cost and posts two balanced entries — Dr Cash / Cr Retail Revenue (4300) and Dr COGS (5300) / Cr Inventory (1300); oversell-guarded; void restores stock. |
| M29 | Project Management | ✅ | Projects/contracts (`PRJ-` sequence) grouping freight orders, tracked against a planned budget. Live budget-vs-actual P&L rolls up revenue (Σ freight+demurrage) and cost (Σ trip base costs + expenses) from linked orders; guarded lifecycle; attach/detach orders. |

### Analytics & KPI Dashboard (client "ERP Features Updates" spec)

The operations dashboard (`/dashboard`, served by `getKpiDashboard`) exposes the client's KPI spec across five categories. Pure formulas live in `backend/src/services/dashboard-kpi.ts` (unit-tested); the impure aggregator feeds them.

| Category | KPIs | Status |
|----------|------|--------|
| Operational & Delivery | On-Time Delivery %, Avg Transit Time, Load Fill Rate, **Truck Turnaround**, **Damage & Claim Rate**, Delivered Orders | ✅ |
| Cost & Profitability | Cost/km, Revenue/km, Freight Cost/Shipment, Empty-Load Rate | ✅ |
| Fleet & Asset Utilization | Asset Utilization, Fuel Efficiency, Maintenance/km, Breakdown Rate | ✅ |
| Driver & Safety | Active Drivers, Driver Turnover, Total Drivers | ✅ |
| Customer Service & Back-Office | Billing Accuracy, AR Recovery, Outstanding AR, **CSAT**, **NPS** | ✅ |

**Internal capture models (Tier C, 2026-07):** `DockEvent` (arrival/departure → turnaround), `DamageReport` (`DMG-` sequence, status lifecycle → damage/claim rate), `CustomerFeedback` (CSAT 1–5 / NPS 0–10). Each has its own admin screen under Operations and is gated by the `kpi:read`/`kpi:write` permissions. **External-feed KPIs (real telematics idle time, e-invoicing accuracy, credit scoring) remain integration-gated (PRD §5).**

### System & Shared (M30–M36)

| # | Module | Status | Notes |
|---|--------|--------|-------|
| M30 | Common (GPS waypoints, UoM) | 🟡 | GPS waypoint registry (named checkpoints/borders/weighbridges + lat/long) built. **Units-of-measure registry not yet.** |
| M31 | Audit Workbench | ✅ | `ActivityLog` records every write. Field-level old→new diff (§7) is captured via `diffFields`/`logFieldChanges` (`backend/src/lib/activity.ts`) — only changed columns, normalized (Date→ISO, Decimal→string) — and rendered as a before→after diff in the Activity Log screen. Wired into the master-data update routes (clients, drivers, vehicles); the same helper drops into any other update route. |
| M32 | Demo / Sandbox | 🟡 | Seed data exists; **no isolated sandbox partition.** |
| M33 | Master Planning | ✅ | Planner-entered demand forecasts per period + corridor (loads & tonnage), one per (period, corridor). A computed capacity plan compares confirmed demand against live available-fleet capacity (excludes maintenance, honors per-forecast truck overrides) and surfaces per-corridor shortfall/surplus & utilization. |
| M34 | Organization Admin | ✅ | `Company` legal-entity registry with admin CRUD screen + company switcher; users assigned a home company; `dataAreaId` partitioning enforced at the query layer. **Fiscal calendar / period-close** per entity: close a month (`FiscalPeriod`, Accounting Periods screen) and the ledger refuses any new posting dated in it — enforced in `createJournalEntry`, so all posting types (invoices, POS, payroll, revaluation…) respect it. |
| M35 | Project Oaktree | ⬜ | (unspecified in PRD detail) |
| M36 | System Administration | ✅ | User management, account active/inactive, and **dynamic RBAC**: a Roles & Permissions admin screen to create custom roles, edit any role's permission grants via a resource-grouped matrix, and assign a custom role to a user. The 5 built-in roles are seeded (`isSystem`, protected from deletion) but re-permissionable. Grants live in `rbac_*` tables and hydrate an in-memory map so `can()` stays synchronous (no route changes); the JWT carries the effective role key. |

## Foundation gaps (contractual, PRD §6–§7)

Status of the Phase-1 "non-negotiable" requirements:

1. ✅ **`Row_Version` optimistic concurrency** (§7.1) — `version Int @default(0)` on business models, enforced via `updateWithVersion()`/`requireVersion()` (`backend/src/lib/concurrency.ts`).
2. ✅ **Audit metadata** (§7.1) — `createdById` / `updatedById` on business tables; writes recorded in `ActivityLog`.
3. ✅ **Enforced `dataAreaId` partitioning** (§6.2) — every business record carries `dataAreaId`, enforced at the query layer (`backend/src/lib/scope.ts`); cross-company reads return 404 for non-admins.
4. ✅ **Multi-currency revaluation** (M8) — period-end run restates open foreign AP/AR to the period-end rate and posts the net unrealized FX gain/loss (`revaluePeriod`, `POST /api/fx/revalue`, preview + post from the Exchange Rates screen).

## External integrations (PRD §5) — all pending

KRA TIMS, TRA VFD, M-Pesa, Airtel Money, GPS tracking, biometric terminals, CRDB/KCB/Stanbic bank feeds. Data models are in place to receive callbacks (e.g. `MoneyTransfer.status`, `externalRef`); **no live API clients are implemented.** Gated on client-provided sandbox credentials (PRD §9.3 Open Items #2, #4, #5).
